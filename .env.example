# Development database (with local PostgreSQL container)
DATABASE_URL_DEV="postgresql://postgres:p0stgres@postgres:5432/cao"
# Production database (external PostgreSQL) - Use GitHub Secrets for actual deployment
DATABASE_URL="postgresql://username:password@host:port/database_name"
JWT_SECRET="your-jwt-secret-here"